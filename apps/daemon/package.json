{
  "name": "@kraken/daemon",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run --bin kraken -- daemon run",
    "format": "cargo fmt",
    "lint": "cargo clippy -- -D warnings",
    "typecheck": "cargo check",
    "test": "cargo test"
  }
}
