{
  "name": "@kraken/scheduler",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "lint": "cargo clippy -- -D warnings",
    "typecheck": "cargo check",
    "test": "cargo test"
  }
}
